# MG Intelligence Hub v3.2 — 项目规划

> 版本：v3.2 | 2026-06-17
> 定位：MA-MG-HUB 医学事务 AI 变革引擎 — 围绕 MSL 工作流的主动赋能系统
> 运行形态：**静态站（GitHub Pages，已上线）+ 轻量后端（阶段 3 MVP 后按需引入）**
> 状态：阶段 0 已实施 ✅，阶段 1-7 静态站 MVP 已落地；轻量后端、LLM 高保真提取、医学/合规复核流程按验证结果继续增强

---

## 🚀 实施进度速览

| 阶段 | 状态 | 说明 |
|:--|:--|:--|
| **0** LLM 引擎层 + P0 修复 | ✅ **已完成**（2026-06-17） | llm_client.py + 脚本修复 + 冒烟测试通过 |
| 1 情报中心 | ✅ 静态站 MVP 已完成 | 文献速览 + 信号板 + 中国情报 |
| 2 专家画像 | ✅ 静态站 MVP 已完成 | 160 位专家画像 + 检索 + 画像详情 |
| 3 MSL 闭环 | ✅ 本地 MVP 已完成 | 拜访简报 + localStorage 录入 + Follow-up；后端待验证后引入 |
| 4 内容工坊 | ✅ 静态站 MVP 已完成 | 模板 + 模块选择 + 草稿组装 + 合规检查 |
| 5 诊治格局 | ✅ 静态站 MVP 已完成 | 证据矩阵 + 治疗通路 + 中外差异 + 竞争管线 |
| 6 Dashboard | ✅ 已完成 | 首页信号/待办/快速入口/团队概览 |
| 7 自动化流水线 | ✅ 骨架已完成 | run-weekly-pipeline + GitHub Actions；外部通知与后端待配置 |

---

## 修订说明（相对 v3-clean 的变更）

| 变更项 | v3-clean | v3.2 | 原因 |
|:--|:--|:--|:--|
| 新增阶段 0 | 无 | **LLM 引擎层基础设施** | 6+ 脚本都要调 LLM，无统一封装会重复造轮子 |
| 运行形态 | 未明确 | **静态站 + 轻量后端（阶段3 MVP后按需引入）** | 拜访记录写入、内容组装先走本地 gitignore 存储，验证后再按需引入后端 |
| 工作量估算 | 12 会话 | **20-24 会话** | v3 严重低估（LLM 层 0% 复用、前端交互层 0% 复用） |
| 附录数据口径 | 仅近1年 | **近1年 + 全库双口径** | 避免对成熟度的误判 |
| 章节顺序 | 按文档历史顺序 | **按阶段号顺序** | v3 的 §9=阶段1、§11=阶段3、§13=阶段5 顺序混乱 |
| 合规隔离 | 未提及 | **拜访数据 gitignore + 后端存储** | 专家个人信息/商业敏感数据不入公开仓库 |
| DOI 缺失 | 未提及 | **改为可选字段** ✅ | 全库 DOI=0；引用溯源统一以 PMID 为主键，DOI 仅作为可选增强字段，不作为任何阶段的阻断依赖 |
| 脚本依赖断裂 | 未提及 | **阶段 0 已修复** ✅ | 两脚本引用已删除的 literature-2026.json，已在阶段 0 修复 |
| v2 痕迹 | 残留 | **清除** | clean 版仍有 "v2 本质" 等字样 |

---

## 一、核心理念（不变）

MG Intelligence Hub 是**情报驱动型**系统 — 用户（MSL / MA）在系统中工作，系统实时增强用户。四大工作流：情报感知 → 知识管理 → 学术产出 → MSL 闭环。

```
┌──────────────────────────────────────────────────┐
│                  MG Intelligence Hub v3.2          │
│                                                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────┐ │
│  │ 情报感知   │  │ 知识管理   │  │ 学术产出   │  │ MSL│ │
│  │ (Signal)  │  │ (Know.)  │  │ (Content)│  │ 闭环│ │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └──┬─┘ │
│       └──────────────┼──────────────┼──────────┘    │
│                      ▼              ▼                │
│              ┌────────────────────────┐              │
│              │  LLM 引擎层 + 数据管线   │              │
│              └────────────────────────┘              │
└──────────────────────────────────────────────────┘
```

### 关键设计原则（修订版）

1. **工作流优先于页面** — 先画用户日常流程，再决定页面结构
2. **信号驱动，不展示驱动** — Dashboard 回答"这周该干什么"
3. **LLM 做提取不做生成** — 提取/归类/结构化交给 LLM；最终产出的内容和判断由人决定
4. **渐进可部署** — 每阶段独立可用
5. **中国优先** — 所有情报通道都有中国独立视角
6. **反馈闭环** — MSL 使用行为反哺系统优化
7. **🆕 LLM 层统一封装** — 所有 LLM 调用走同一客户端（模型/重试/成本/缓存）
8. **🆕 数据合规隔离** — 公开数据入仓库，敏感数据（拜访/专家内部标签）走 gitignore 或后端

---

## 二、四大工作流详解

### 工作流 A：情报感知（Signal Intelligence）

| 维度 | 当前状态 | 目标 |
|:--|:--|:--|
| PubMed 文献 | ✅ 14天窗口抓取+展示 | 增加**新研究信号提取**（什么机制热了、哪个靶点有突破） |
| 会议信息 | ⏳ 待启动 | 公开摘要（AAN/EAN/AANEM）自动抓取 → 信号入库 |
| 中国信号 | ✅ 全库 1,866 篇可过滤 | PubMed 中国研究过滤 + 手动录入指南/政策更新 |
| MSL 一线情报 | ❌ 无 | 拜访后录入 → LLM 自动归类（证据缺口/观念变化/竞争动态） |
| 监管动态 | ⏳ 待启动 | FDA/NMPA 批准时间线，自动关联到相关文献和试验 |

**核心理念：** 情报不是"这周发了哪些文章"的展示，而是**"什么在变化"的信号推送**。

**输出形态：**
- Dashboard 的"本周信号"区块 — 每次打开看到 3-5 条值得关注的变化
- 每周自动简报（支持飞书 webhook 推送）
- 信号按强度/紧急度排序

### 工作流 B：知识管理（Knowledge Foundation）

| 维度 | 当前状态 | 目标 |
|:--|:--|:--|
| efgartigimod-wiki | ✅ 900页，独立存在 | 打通：Wiki 知识直接可被 HUB 检索/引用 |
| 暗数据 | ❌ 无 | MSL 访谈录音/笔记 → 自动结构化入库 |
| 文献回填 | ✅ 全库 68% 有证据等级、67% 有 IF | 增加**证据矩阵** — 某个问题有哪些证据支持/反对 |
| 专家知识库 | ✅ 39,334 作者基础数据 | 专家画像 + 拜访记忆时间线 + Follow-up 请求清单 |

**核心理念：** 知识是**可检索、可关联、可复用、带记忆**的资产。

### 工作流 C：学术内容产出（Content & Creation）

| 维度 | 当前状态 | 目标 |
|:--|:--|:--|
| 快速简报 | ✅ 一键生成（简报篮） | 增加**模板选择**（文献速递/竞品简报/会议摘要集） |
| 内容模块化 | ❌ 无 | 核心模块库：药理模块、临床数据模块、安全性模块、指南比较模块 |
| 按需组装 | ❌ 无 | 选择模板 → 选模块 → 输入参数 → 生成初稿 → 人工终审 |
| 合规校验 | ❌ 无 | 自动检查引用来源、数据一致性、声明格式 |

**核心理念：** 内容模块化 + 按需组装，让 MSL 在 10 分钟内生成个性化的学术材料。

### 工作流 D：MSL 学术互动闭环（Medical Engagement Loop）

**最核心、最能直接提升 MSL 团队价值**的工作流。

**核心设计：记忆感知的循环模式。** 区分**新拜访**与**回访**，让每次拜访都建立在真实的历史互动上，形成反馈→衍生→行动的飞轮。

```
                    ┌──────────────────────────────────────────┐
                    │              专家知识库                    │
                    │  发文历史 · 合作网络 · 画像               │
                    └──────────┬──────────────────┬────────────┘
                               │                  │
          ┌────────────────────┘                  └──────────────────┐
          ▼                                                          ▼
┌─────────────────────────────┐                     ┌─────────────────────────────┐
│  新拜访（首次/隔了很久）     │                     │  回访（有历史互动）          │
│  ① 专家画像概览              │                     │  ① 上次拜访纪要回顾          │
│  ② 近期热点关联性分析        │                     │  ② Follow-up 清单（自动追踪）│
│  ③ 学术合作机会提示          │                     │  ③ 专家索要材料准备度        │
│                              │                     │  ④ 新 Topic 衍生信号          │
│  → 生成「全新拜访简报」       │                     │  → 生成「跟进拜访简报」        │
└─────────────────────────────┘                     └─────────────────────────────┘
                               │                  │
                               └────────┬─────────┘
                                        ▼
                              ┌──────────────────┐
                              │    拜访结束后     │
                              │  结构化表单录入    │
                              │  LLM 归类 → 信号   │
                              └────────┬─────────┘
                                       │
                                       ▼
                     ┌──────────────────────────────────────┐
                     │  专家知识库更新 · 一线证据入库 · 团队汇总 │
                     └──────────────────┬───────────────────┘
                                        └────→ 成为下次拜访的「记忆输入」
```

**"回访"模式的核心价值：**

| 要素 | 新拜访 | 回访 |
|:--|:--|:--|
| 起点 | 专家画像+热点 | 上次拜访纪要 |
| 追踪项 | 无 | Follow-up 清单 |
| 准备度 | 通用材料 | 专家索要的具体数据/文献 |
| 衍生信号 | 泛泛的热点 | 基于上次讨论自然衍生的新 topic |
| 关键差异 | 一次性 | **循环——本次产出成为下次输入** |

**⚠️ 成败关键（v3 新增提示）：** 此闭环的价值完全依赖**团队持续录入拜访数据**。若录入率低，回访简报退化为"画像+热点"。阶段 3 采用先做最小闭环 MVP 的策略（见阶段 3）。

---

## 三、技术架构（修订版）

### 运行形态：静态站 + 轻量后端

```
┌─────────────────────────────────────────────────────────┐
│                    用户浏览器                             │
│  GitHub Pages 静态站（HTML/CSS/原生JS + Alpine.js）       │
│  数据：.js 文件（literature/signals/expert/landscape）    │
└──────────────┬───────────────────────┬──────────────────┘
               │ 只读加载 .js          │ 交互（POST）
               ▼                       ▼
┌──────────────────────┐   ┌─────────────────────────────┐
│  GitHub Pages        │   │  轻量后端（阶段3 MVP后按需）   │
│  （展示层，只读）     │   │  Cloudflare Workers /        │
│                      │   │  Vercel Functions            │
│  文献/信号/格局展示   │   │  · LLM 调用代理              │
│  专家画像展示         │   │  · 拜访记录写入（KV/D1）      │
│                      │   │  · 内容组装                  │
└──────────────────────┘   └─────────────────────────────┘
                                       │
                                       ▼
                           ┌─────────────────────────────┐
                           │  LLM API（DeepSeek-V3）       │
                           └─────────────────────────────┘
```

**职责划分：**
- **静态站（GitHub Pages）**：所有"看"的功能 — 文献浏览、信号板、专家画像展示、证据矩阵展示、Dashboard
- **轻量后端**：所有团队化的"写"和"实时生成"功能 — 拜访记录录入、简报生成、内容组装、LLM 调用代理（隐藏 API key）
- **阶段 3 MVP 口径**：先用本地 gitignore JSON 验证录入率和简报质量；只有当 MVP 证明有持续使用价值后，再迁移到 Cloudflare Workers / Vercel Functions。

### LLM 引擎层（新增 — 阶段 0）

**核心：所有 LLM 调用走统一封装，避免每个脚本各自处理。**

```
scripts/llm_client.py
├── 统一调用接口：llm_complete(prompt, model, temperature)
├── 重试机制（指数退避，最多 3 次）
├── 本地缓存（prompt hash → 响应，避免重复调用）
├── 成本日志（token 计数 + 累计花费）
└── 模型路由（高频低质 → DeepSeek-V3；低频高质 → 更强模型）
```

**配置管理：**
- API key 存环境变量（本地）/ GitHub Actions Secrets（管线）
- 模型选择：主用 DeepSeek-V3（DeepSeek 官方 OpenAI 兼容 API），成本可控
- 缓存目录：`data/.llm_cache/`（gitignore）

### 数据管线流程

```
每周管线（GitHub Actions cron）：
  fetch-pubmed → full.json
    → study-classification (证据等级)
    → journal-metrics (IF/分区)
    → llm-signal-extraction（LLM 逐篇提取）
    → signal-aggregation（跨文献聚合）
    → build-expert-profiles
    → build-china-intelligence
    → extract-landscape-signals
    → build-evidence-matrix
    → split-recent-data → recent.js
    → build-frontend-data → signals/china/expert/landscape 各 .js
    → module-update-check
    → git push（自动部署）

MSL 拜访（按需，先本地存储）：
  visit-input（前端表单）
    → llm-structure（自由文本→结构化）
    → 本地写入（data/visit-records.json，gitignore）
    → signal-update + expert-profile-update

  （阶段 3 MVP 验证后，按需迁移到轻量后端）

内容产出（按需，先本地组装）：
  select-template + select-modules
    → llm-compose（组装初稿）
    → compliance-check
    → human-review → final output

  （阶段 4 验证后，按需引入后端加速）
```

---

## 四、路线图（修订版）

| 阶段 | 内容 | 交付 | 前置 | 估算 | 当前状态 |
|:--|:--|:--|:--|:--|:--|
| **0** | **🆕 LLM 引擎层 + P0 修复** | llm_client.py 统一封装 + 脚本依赖修复 | 无 | 2 会话 | ✅ 已完成 |
| **1** | **重构情报中心** | 文献速览保留 + 新增**信号板** + 中国情报独立模块 | 阶段 0 | 3-4 会话 | ✅ 静态站 MVP 已完成 |
| **2** | **MSL 专家画像** | 专家数据模型 → PubMed 回填 → 画像看板 | 阶段 0 | 2 会话 | ✅ 静态站 MVP 已完成 |
| **3** | **MSL 拜访闭环**（先 MVP） | 专家画像 + 拜访记忆底座 + 新拜访/回访简报 + Follow-up + 结构化录入 + 团队汇总 | 阶段 0+1+2 | 4-5 会话 | ✅ 本地 MVP 已完成；后端验证后引入 |
| **4** | **内容工坊** | 模板库 + 模块管理 + 按需组装 → 合规校验 | 阶段 0+1 | 3-4 会话 | ✅ 静态站 MVP 已完成 |
| **5** | **诊治格局升级** | LLM 提取层 + 中外差异信号 + 证据矩阵 | 阶段 0+1 | 3 会话 | ✅ 静态站 MVP 已完成；医学复核后增强 |
| **6** | **Dashboard + 整合** | 全模块联动 → 首页信号/待办/团队面板 | 阶段 1-5 | 1 会话 | ✅ 已完成 |
| **7** | **自动化流水线** | GitHub Actions cron + 通知 + 稳定化 | 阶段 1-6 | 2-3 会话 | ✅ 骨架已完成；通知待配置 |
| | | | **合计** | **20-24 会话** | ✅ 静态站 MVP 全部落地 |

---

## 五、页面结构（修订版）

### 当前落地状态（2026-06-17）

| 页面 | 当前状态 |
|:--|:--|
| `index.html` | ✅ Dashboard 已重构：信号、待办、快速入口、团队概览 |
| `pages/literature.html` | ✅ 情报中心已升级：文献速览、信号板、中国情报 |
| `pages/knowledge.html` | ✅ 新增知识库：文献、专家、证据问题统一检索 |
| `pages/msl.html` | ✅ 新增 MSL 工作台：专家画像、拜访简报、本地 Follow-up |
| `pages/materials.html` | ✅ 内容工坊已重构：模板、模块、草稿组装、合规检查 |
| `pages/landscape.html` | ✅ 诊治格局已重构：证据矩阵、治疗进展、中外差异、竞争格局 |
| `pages/data-ops.html` | ✅ 数据状态已动态化：公开数据产物、流水线命令、更新状态 |
| `pages/competitive.html` | ✅ 已合并入诊治格局，并保留跳转页 |
| `pages/outputs.html` | ✅ 已合并入内容工坊，并保留跳转页 |
| `pages/progress.html` | ✅ 已合并入内容工坊，并保留跳转页 |

### 目标全局导航

```
导航栏（统一）：
  工作台(Dashboard) | 情报中心 | 知识库 | MSL工作台 | 内容工坊 | 诊治格局 | 数据状态

页面映射：
  工作台      → index.html（阶段 6 重构）
  情报中心    → pages/literature.html（阶段 1 升级：+信号板+中国情报）
  知识库      → pages/knowledge.html（新建：Wiki检索+文献档案+一线证据）
  MSL工作台   → pages/msl.html（新建：专家画像+拜访助手+Follow-up+团队面板）
  内容工坊    → pages/materials.html（阶段 4 改造：模板库+模块+组装+历史）
  诊治格局    → pages/landscape.html（阶段 5 改造：证据矩阵+治疗进展+中外差异+竞争格局）
  数据状态    → pages/data-ops.html（保留，数字动态化）
```

**已合并并保留入口：** `pages/competitive.html`、`pages/outputs.html`、`pages/progress.html` 均保留为提示/跳转页，避免旧链接断裂；实际能力分别进入诊治格局与内容工坊。

---

## 六、和现有系统的关系（诚实版）

- **不是推翻重来** — 文献管线（PubMed 抓取/IF回填/研究分类）、前端框架、GitHub Pages 部署，全部保留
- **变化的是架构** — 从"数据展示"到"工作流驱动"，新增 LLM 引擎层 + 轻量后端
- **复用率诚实评估：**
  - 数据管线层：**~80% 可复用**（fetch/backfill/split 健壮，两脚本已在阶段 0 修复 ✅）
  - LLM 引擎层：**阶段 0 已建设** ✅（`scripts/llm_client.py`，缓存+重试+成本日志）
  - 前端展示层：初始复用约 **30%**；v3.2 已重建为 7 页面静态站 MVP
  - 前端交互层（录入/组装）：初始 **0% 可复用**；v3.2 已补齐本地交互闭环
- **现状（2026-06-17）**：阶段 0-7 静态站 MVP 已落地；轻量后端、外部通知、医学/合规复核属于验证后的增强项。

---

## 七、MSL Manager 视角的 ROI（修订版）

| 能力 | 当前状态 | v3.2 目标 | 对 MSL Manager 的价值 |
|:--|:--|:--|:--|
| 专家画像 | ❌ 人工零散记录 | ✅ 一键查看 | 管理团队覆盖的专家更精准 |
| 拜访前准备 | ❌ 人工翻阅文献 | ✅ AI 5秒生成 | 拜访前准备节省 ~30 min |
| 拜访后整理 | ❌ 自由文本→难复用 | ✅ 结构化→信号入库 | 拜访后录入增加 5-10 min，净节省 20-25 min |
| 团队情报汇总 | ❌ 月度PPT汇报 | ✅ 实时信号看板 | 随时知道团队发现了什么 |
| 学术内容制作 | ❌ 每次从零开始 | ✅ 模块化组装 | 产出速度 2-3×，质量可控 |
| 合规风险 | ❌ 全靠个人判断 | ✅ 自动引用校验 | 降低学术材料合规风险 |

---

## 八、阶段 0 详细设计（🆕 LLM 引擎层 + P0 修复）

> 设计日期：2026-06-16 | 实施日期：2026-06-17
> 状态：✅ **已完成**（2026-06-17）
> 目的：为所有后续阶段的 LLM 调用提供统一基础设施，并修复已发现的阻断项

### 8.0 实施结果（2026-06-17）

**冒烟测试已通过：**
- ✅ DeepSeek API 连通（`https://api.deepseek.com`，OpenAI 兼容协议）
- ✅ 第一次调用 1.1s（真实 API），第二次调用 0.001s（缓存命中）
- ✅ 两次回复一致，缓存工作正常
- ✅ 成本日志已记录，单次调用约 ¥0.0001
- ✅ 密钥存 `~/.config/mg-hub/.env`（home 目录，不进仓库，权限 600）
- ✅ `.gitignore` 已隔离 `.env`、`data/.llm_cache/`、`data/.llm_cost.log`

**实际产出文件：**

| 文件 | 类型 | 说明 |
|:--|:--|:--|
| `scripts/llm_client.py` | 🆕 新建 | 统一 LLM 客户端（密钥加载/缓存/重试/成本日志），约 240 行 |
| `scripts/llm_demo.py` | 🆕 新建 | 冒烟测试脚本 |
| `scripts/backfill-study-classification.py` | ✏️ 修复 | 删除对已删除的 `literature-2026.json` 的依赖 |
| `scripts/normalize-journal-names.py` | ✏️ 修复 | 删除对已删除的 `literature-2026.json` 的依赖 |
| `.gitignore` | ✏️ 更新 | 追加 LLM 相关忽略规则 |
| `~/.config/mg-hub/.env` | 🆕 新建（仓库外） | DeepSeek API key，权限 600 |

**验证命令：**
```bash
# 跑冒烟测试（自动验证 API + 缓存 + 成本日志）
python3 scripts/llm_demo.py

# 直接自检（只验证密钥加载，不实际调用）
python3 scripts/llm_client.py
```

### 8.1 LLM 客户端封装（已实现）

**`scripts/llm_client.py` 核心接口：**

```python
# 模型常量
DEFAULT = "deepseek-chat"        # DeepSeek-V3，日常提取/分类
REASONER = "deepseek-reasoner"   # 需要推理的任务（证据矩阵、复杂判断）

# 核心调用（自动缓存 + 重试 + 成本记录）
def complete(prompt, system=None, model=DEFAULT,
             temperature=0.3, max_tokens=2000, use_cache=True) -> str:
    """调用 LLM 返回文本回复。"""

# 批量调用（逐篇文献提取信号等场景，已缓存的自动跳过）
def complete_batch(items, prompt_builder, model=DEFAULT,
                   temperature=0.3, use_cache=True):
    """prompt_builder(item) -> (prompt, system)。返回 [(item, reply), ...]。"""

# 成本查询
def total_cost() -> float:
    """读取累计花费（人民币元）。"""
```

**特性说明：**

| 特性 | 实现 |
|:--|:--|
| 密钥加载 | 环境变量 `DEEPSEEK_API_KEY` → `~/.config/mg-hub/.env` → 报错提示 |
| 本地缓存 | prompt+model 的 sha256 → `data/.llm_cache/{hash}.json`（命中则不调 API） |
| 重试 | 指数退避（429/5xx），最多 3 次 |
| 成本日志 | `data/.llm_cost.log`（时间/模型/token 数/费用），`total_cost()` 查累计 |
| 模型路由 | DEFAULT（日常）/ REASONER（推理） |
| 定价依据 | DeepSeek 官方：deepseek-chat input ¥1/M、output ¥2/M；reasoner input ¥4/M、output ¥16/M |

**后续脚本用法（两行即用）：**
```python
from llm_client import complete, REASONER

# 日常提取/分类
reply = complete("分析这篇文献的信号", temperature=0.3)

# 需要推理的任务
reply = complete("...", model=REASONER)

# 查累计花费
from llm_client import total_cost
print(f"已花费 ¥{total_cost()}")
```

### 8.2 P0 修复项（已完成）

| 项 | 文件 | 问题 | 修复状态 |
|:--|:--|:--|:--|
| 脚本依赖断裂 | `backfill-study-classification.py` | main() 引用已删除的 `literature-2026.json` | ✅ 已删除 yearly 分支，只回填 `literature-full.json` |
| 脚本依赖断裂 | `normalize-journal-names.py` | save_data() 写已删除的 `literature-2026.json` | ✅ 已删除该写入逻辑 |
| DOI 全库为 0 | `fetch-pubmed-*.py` | DOI 解析逻辑有但产物为空 | ⏭️ **不作为阻断项** — 引用溯源统一以 PMID 为主键；DOI 后续仅作为可选增强字段补充 |

### 8.3 配置与密钥（已落地）

| 环境 | API key 存放 | 状态 |
|:--|:--|:--|
| 本地开发 | `~/.config/mg-hub/.env`（home 目录，权限 600，不进仓库） | ✅ 已配置 |
| GitHub Actions | Repository Secrets（`DEEPSEEK_API_KEY`） | ⏳ 阶段 7 配置 |
| 轻量后端 | 平台环境变量（Cloudflare/Vercel） | ⏳ 阶段 3 配置 |

**⚠️ 安全提示：** API key 曾在对话中明文出现过。虽然已存入 gitignore 的文件不会提交仓库，但建议定期在 DeepSeek 后台轮换 key，更新 `~/.config/mg-hub/.env` 一处即可。

### 8.4 编码顺序（实际执行情况）

| 步骤 | 计划 | 实际 |
|:--|:--|:--|
| 1 | 写 `scripts/llm_client.py` | ✅ 完成 |
| 2 | 修复两个脚本的 `literature-2026.json` 依赖 | ✅ 完成 |
| 3 | 排查 DOI 缺失 | ⏭️ 评估后决定忽略（见 §8.2） |
| 4 | 建立 `data/.llm_cache/` + `.gitignore` 规则 | ✅ 完成 |
| 5 | 写 demo 脚本验证 LLM 调用链路 | ✅ 完成（`scripts/llm_demo.py`） |

---

## 九、阶段 1 详细设计（情报中心重构）

> 设计日期：2026-06-16
> 状态：✅ 静态站 MVP 已完成（2026-06-17）；高保真 LLM 提取可在验证后继续增强
> 前置：阶段 0

### 9.1 信号分类体系

| 类别 | 说明 | 子标签 |
|:--|:--|:--|
| 新证据 | 新临床数据，改变或补充现有认知 | 本品 / 竞品 / 其他 |
| 新机制 | 病理/分子机制新发现 | 无 |
| 新观点 | 指南/共识/专家评论新立场 | 无 |
| 监管信号 | 获批/适应症扩展/标签变更 | 无 |

**注意：** 竞争动态归入"诊治格局"页面独立展示。

**信号强度分级：**
- 🔴 强信号：高影响因子期刊 + 明确改变实践的潜力
- 🟡 中信号：值得了解但未改变格局
- 🟢 弱信号：小众发现，特定场景有用

### 9.2 中国情报

中国情报 = 两个来源：
- **自动**：`literature-recent.json` 中 `china_related=true`（近1年 313 篇，全库 1,866 篇）
- **手动**：`data/china-manual.json` — 手动录入指南/政策更新

```json
{
  "manual_updates": [
    {
      "date": "2026-06-10",
      "type": "指南更新",
      "title": "...",
      "source": "...",
      "url": "...",
      "note": "..."
    }
  ]
}
```

### 9.3 页面结构（情报中心）

3 个标签页：
1. **文献速览**（保留现有 literature.html 功能）
2. **信号板**（新增）— 本周发现的新趋势/新证据/新观点，按强度排序
3. **中国情报**（新增）— PubMed 中国研究过滤 + 手动更新

### 9.4 数据管线

```
fetch-pubmed → full.json
  → study-classification (证据等级)
  → journal-metrics (IF/分区)
  → extract-signals.py → signals-weekly.js  ← 【新增】LLM 逐篇提取 + 跨文献聚合
  → split-recent-data.py → recent.js
  → build-china-intelligence.py → china-intelligence.js  ← 【新增】
  → git push
```

### 9.5 新增数据文件

**`signals-weekly.js`（前端加载）：**
```javascript
window.MG_SIGNALS_DATA = [
  {
    "date": "2026-06-16",
    "signals": [
      {
        "type": "新证据",
        "subtype": "竞品",
        "strength": "强",
        "summary": "...",
        "related_pmids": ["12345678", "87654321"],
        "keywords": ["rozanolixizumab", "FcRn"]
      }
    ]
  }
];
```

**`china-intelligence.js`：**
```javascript
window.MG_CHINA_DATA = {
  "pubmed_articles": [...],
  "manual_updates": [...]
};
```

### 9.6 LLM 提取策略

| 决策 | 选择 |
|:--|:--|
| 单篇提取 | ✅ LLM 逐篇评估"这篇文献值得关注吗"，提取 0-N 个信号 |
| 跨文献聚合 | ✅ 同一主题本周出现 N 次 → 提升信号强度 |
| 模型 | DeepSeek-V3（经 llm_client.py 路由） |
| 成本估算 | 每周 50-100 篇新文献 × 500-800 token < $0.5/周 |

### 9.7 编码顺序与实际落地

1. ✅ 当前 MVP：`scripts/build-frontend-data.py` 统一生成 `signals-weekly.js` 与 `china-intelligence.js`
2. ✅ `split-recent-data.py` 保持单一职责，只负责近 1 年文献切分与 `literature-recent.js`
3. ✅ 前端：`literature.html` 新增"信号板"+中国情报标签
4. ✅ Dashboard 首页接入本周信号区块
5. 后续增强：如需高保真 LLM 信号提取，可再拆分 `extract-signals.py` 与 `build-china-intelligence.py`

---

## 十、阶段 2 详细设计（MSL 专家画像）

> 设计日期：2026-06-16
> 状态：✅ 静态站 MVP 已完成（2026-06-17）；核心专家人工名单可在内部数据层继续补充
> 前置：阶段 0

### 10.1 数据源

| 数据源 | 状态 | 说明 |
|:--|:--|:--|
| PubMed 发文 | ✅ 唯一数据源 | 从 `literature-full.json` 提取（39,334 作者，556 人发文≥10篇） |
| ClinicalTrials | ❌ 不接入 | 数据不准确 |
| 会议演讲 | ❌ 不接入 | 数据不准确 |
| ORCID / h-index | ❌ 不接入 | 无权威数据源 |

### 10.2 专家识别方式

**方案 C：姓名+机构消歧 + 手动标注核心专家**

系统维护 `data/experts-core.json`（核心专家名单，**gitignore，不入公开仓库**）：

```json
{
  "experts": [
    {
      "id": "expert_001",
      "name_zh": "张三",
      "name_en": "Zhang San",
      "affiliation": "北京协和医院",
      "tags": ["FcRn", "重症肌无力"],
      "priority": "A",
      "msl_owner": "李明",
      "status": "active"
    }
  ]
}
```

### 10.3 专家画像展示

```
┌─────────────────────────────────────────┐
│  张三 · 北京协和医院 · 神经免疫科          │
│  Priority: A  ·  MSL: 李明               │
├─────────────────────────────────────────┤
│  📊 学术指标                              │
│  总发文 47 篇  ·  近3年 12 篇              │
│  最高IF 15.3  ·  主要期刊 3 种              │
├─────────────────────────────────────────┤
│  🔬 研究兴趣向量（词频统计）                │
│  FcRn ████████░░  8篇                    │
│  补体 █████░░░░░  5篇                    │
├─────────────────────────────────────────┤
│  📅 时间线                                │
│  2026-05  Nature Neurosci  FcRn新机制      │
├─────────────────────────────────────────┤
│  🤝 关联网络（简单版）                      │
│  主要合作者：李四(12篇) · 王五(8篇)         │
├─────────────────────────────────────────┤
│  📝 内部互动记录（拜访历史）                │
│  2026-06-10  拜访 · 关注FcRn长期安全性      │
└─────────────────────────────────────────┘
```

### 10.4 研究兴趣向量

**先用简单版（词频统计）验证有用性：**
1. 提取专家所有发文的标题+摘要
2. 分词 → 过滤停用词
3. 统计词频（TF）
4. 取 top 10

**后续升级：** TF-IDF 加权、LLM 主题提取（验证 MSL 觉得词频版有用后再做）。

### 10.5 新增数据文件

```
data/
  experts-core.json           ← 手动维护（gitignore，敏感）
  expert-profiles.js          ← 管线自动生成（可公开的部分）
  expert-internal.js          ← 管线自动生成（含拜访记录，gitignore）
```

### 10.6 编码顺序与实际落地

1. ✅ 当前 MVP：`scripts/build-frontend-data.py` 从公开文献数据生成 `expert-profiles.js`
2. ✅ 前端：MSL 工作台新增专家画像检索、列表与详情
3. ✅ 拜访简报使用专家画像 + 本周情报生成本地草稿
4. 后续增强：如需人工核心专家名单与内部标签，补充 gitignore 的 `data/experts-core.json`

---

## 十一、阶段 3 详细设计（MSL 拜访闭环）

> 设计日期：2026-06-16
> 状态：✅ 本地 MVP 已完成（2026-06-17）；团队后端与多人协作待验证后引入
> 前置：阶段 0+1+2
> ⚠️ 策略：**先做本地最小闭环 MVP，验证录入率后再决定是否引入轻量后端**

### 11.1 MVP 范围（第一轮）

**先做：**
- 单 MSL、3-5 位核心专家
- 新拜访简报生成（画像+热点）
- 结构化录入表单（前端 → 本地 `data/visit-records.json`，gitignore）
- Follow-up 列表（按 deadline 排序）

**验证后再做：**
- 轻量后端写入（Cloudflare Workers / Vercel Functions + KV/D1）
- 回访简报（基于历史纪要 + 衍生信号）
- 团队信号汇总（多人聚合）
- 超时警示

### 11.2 拜访记录数据结构

```json
{
  "visit_id": "v_20260610_001",
  "expert_id": "expert_001",
  "expert_name": "张三",
  "msl_name": "李明",
  "date": "2026-06-10",
  "type": "回访",
  "location": "北京协和医院",
  "pre_brief": {
    "generated_at": "2026-06-09",
    "content": "..."
  },
  "discussion": {
    "topics": [
      {
        "topic": "FcRn 长期安全性",
        "expert_view": "专家关注...",
        "our_response": "我们提供了...",
        "follow_up_needed": true,
        "follow_up_items": ["提供 2 年随访数据"]
      }
    ],
    "new_signals": [
      {"type": "观念变化", "content": "专家对补体抑制剂态度从观望转积极"}
    ],
    "materials_requested": [
      {"item": "Efgartigimod 日本真实世界数据", "status": "待准备", "deadline": "2026-06-20"}
    ]
  },
  "post_summary": {
    "key_takeaways": ["..."],
    "next_visit_suggested": "2026-07-10"
  }
}
```

**存储位置：** 🔴 拜访记录含个人信息/商业敏感数据，MVP 阶段存本地 gitignore 文件；验证通过后迁移到轻量后端（KV/D1）。无论哪种方式，**绝不入公开 GitHub 仓库**。

### 11.3 拜访前简报生成

**新拜访模式：**
- 输入：专家画像 + 本周情报信号
- 输出：专家概览 + 近期热点关联 + 学术合作机会

**回访模式（MVP 后）：**
- 输入：上次拜访记录 + 本周情报 + 专家画像更新
- 输出：上次纪要回顾 + Follow-up 清单 + 材料准备度 + 新 Topic 衍生信号

### 11.4 Follow-up 追踪系统

```json
{
  "backlog": [
    {
      "request_id": "req_001",
      "expert_id": "expert_001",
      "item": "Efgartigimod 日本真实世界数据",
      "status": "待准备",
      "deadline": "2026-06-20",
      "owner": "李明"
    }
  ]
}
```

**状态流转：** 待准备 → 已发送 → 已收到反馈 → 已关闭（deadline 过 3 天未更新 → 红色警示）

### 11.5 拜访后信号提取

LLM 自动从拜访记录提取：

| 信号类型 | 去向 |
|:--|:--|
| 观念变化 | 全局信号库 + 专家档案 |
| 证据缺口 | 全局信号库 + 证据矩阵 |
| 竞争动态 | 竞争情报库 |
| 未满足需求 | Follow-up backlog |
| 新合作机会 | 专家档案 + 团队面板 |

### 11.6 团队信号汇总（MVP 后）

所有 MSL 的拜访信号由 LLM 聚合，发现共性趋势（3+ 位 MSL 提到）与区域差异。

### 11.7 页面结构（MSL 工作台）

```
MSL 工作台
├── 专家画像（列表+搜索+画像卡片）
├── 拜访助手
│   ├── 新建拜访（选专家→选类型→生成简报→录入反馈）
│   └── 拜访记录历史
├── Follow-up 追踪（全部待办/按专家/超时警示）
└── 团队面板（MVP 后）
```

### 11.8 编码顺序

1. 创建本地敏感数据模板：`data/visit-records.json`、`data/follow-up-backlog.json`（均 gitignore）
2. 拜访记录数据结构 + 本地读写适配层
3. 前端录入表单（结构化，用 Alpine.js 简化交互；目标 3 分钟内完成关键字段）
4. 新拜访简报生成（LLM）
5. Follow-up 追踪页面
6. **→ MVP 验证点：让 1 位 MSL 用 2 周，看录入率、录入耗时与简报质量**
7. （验证后）轻量后端搭建（Cloudflare Workers / Vercel Functions）+ KV/D1 存储
8. （验证后）回访简报 + 团队汇总

---

## 十二、阶段 4 详细设计（内容工坊）

> 设计日期：2026-06-16
> 状态：✅ 静态站 MVP 已完成（2026-06-17）；模块医学复核与后端组装可继续增强
> 前置：阶段 0+1

### 12.1 内容模块定义

**模块 = 可复用的知识单元。**

| 模块类型 | 参数化 | 初始状态 |
|:--|:--|:--|
| 药理机制 | 药物名称 | 从现有文献提取 Efgartigimod / Rozanolixizumab |
| 临床数据 | 药物、适应症、对比对象 | 填充 Efgartigimod gMG/OMG |
| 安全性 | 药物、随访时长 | 填充 Efgartigimod 安全性 |
| 指南比较 | 指南版本、适应症 | 待证据补全（`verified=false`，需人工复核后发布） |
| 竞品对比 | 对比药物组合 | 填充 Efgartigimod vs Rozanolixizumab |
| 真实世界 | 药物、数据来源 | 待证据补全（`verified=false`，需人工复核后发布） |

**每个模块 = 结构化文本 + 引用来源（PMID 必填，DOI 可选）+ 最后更新日期 + verified 字段。**

**引用主键口径：**
- PMID 是所有文献引用、证据矩阵、内容模块、合规校验的强主键。
- DOI 仅作为可选增强字段；当前全库 DOI=0 不阻断内容工坊。
- 前端展示引用时优先显示 PMID/PubMed 链接；若后续补到 DOI，再追加 DOI 链接。

### 12.2 模板定义

| 模板 | 适用场景 | 包含模块 |
|:--|:--|:--|
| 文献速递简报 | 内部团队分享 | 文献摘要 + 信号解读 + 临床意义 |
| 拜访材料 | 单专家拜访 | 药理 + 临床 + 安全性 + 竞品（按需） |
| 区域学术会 | 区域 KOL 会议 | 治疗格局 + 本品数据 + 指南更新 |
| 竞品应对 | 竞品上市/数据 | 竞品数据 + 本品差异化 |
| 医学部内部 | 策略讨论 | 证据矩阵 + 信号汇总 |

### 12.3 组装流程

```
选择模板 → 选择模块（勾选）→ 输入参数 → 生成初稿 → 合规校验 → 人工终审 → 输出
```

### 12.4 合规校验

| 检查项 | 方式 |
|:--|:--|
| 引用来源存在（PMID 在文献库中；DOI 如有则校验） | 规则 |
| 数据一致性 | 规则 |
| 声明格式（带证据等级） | 规则 |
| 最新性（90 天内） | 规则 |
| 超说明书暗示 | LLM |
| 未复核内容检查 | 规则 |

**⚠️ 合规规则集需医学/合规人员确认后再写代码。** 阶段 4 的工程实现只做规则执行和风险标记，不替代医学/合规终审；未 review 的模块必须显示 `verified=false` 或"待核实"。

### 12.5 编码顺序与实际落地

1. ✅ 当前 MVP：`scripts/build-frontend-data.py` 生成 `content-modules.js`
2. ✅ 前端：`materials.html` 已改造为内容工坊，支持模板选择、模块选择、草稿生成与合规检查
3. ✅ 引用口径统一使用 PMID；未复核内容通过 `verified=false` 明确标记
4. 后续增强：如需要模块版本管理、CRUD 与后端组装，再拆分 `extract-module-data.py`、`module-manager.py`、`compliance-check.py` 与 `composer.py`

---

## 十三、阶段 5 详细设计（诊治格局升级）

> 设计日期：2026-06-16
> 状态：✅ 静态站 MVP 已完成（2026-06-17）；证据等级与竞争信息待医学/商业复核后继续增强
> 前置：阶段 0+1（建议先补一轮 Unclassified 分类）

### 13.1 核心目标

从"展示数据"升级为"回答结构性问题"——某个领域/靶点的全局图景。

### 13.2 证据矩阵

**回答的问题：** "关于 X 问题，有哪些证据支持/反对？"

**预设问题框架：**

| 类别 | 示例 |
|:--|:--|
| FcRn 通路 | Efgartigimod 长期疗效？FcRn 拮抗剂安全性谱？ |
| 补体通路 | C5 抑制剂疗效对比？Zilucoplan vs Ravulizumab？ |
| B细胞通路 | CD20 单抗残留风险？ |
| 传统免疫 | 激素减量方案？MMF vs AZA？ |
| 治疗策略 | 一线选择？难治性序贯治疗？ |
| 特殊人群 | 胸腺瘤相关 MG？儿童 MG？ |
| 真实世界 | 停药/重启策略？联合用药安全性？ |

```json
{
  "question": "Efgartigimod 在 gMG 的长期疗效",
  "evidence_matrix": [
    {"type": "支持", "level": "I", "source": "ADAPT+ 2年随访", "pmid": "...", "key_finding": "...", "limitations": "..."}
  ],
  "summary": "...",
  "verified": false
}
```

**⚠️ 每个矩阵带 `verified` 字段，区分 LLM 生成 vs 医学 review。未 review 的标记"待核实"。**

### 13.3 中外差异

| 维度 | 中国 | 欧美 | 差异 |
|:--|:--|:--|:--|
| 获批状态 | 已获批/审评中 | 已获批 | 时间差 |
| 适应症 | gMG | gMG+扩展 | 范围差 |
| 指南推荐 | 二级 | 一级 | 积累阶段差 |
| 医保覆盖 | 谈判中 | 已纳入 | 支付差 |

**数据来源：** 手动维护为主（`data/china-manual.json`）。

### 13.4 竞争格局

管线对比表（FcRn + 补体 + B细胞），自动更新 + 手动维护结合。

### 13.5 页面结构（诊治格局）

```
诊治格局（landscape.html 改造）
├── 证据矩阵（预设问题 + 支持/反对/待验证 + verified 标记）
├── 治疗进展（FcRn/补体/B细胞/传统免疫）
├── 中外差异（表格 + 时间线可视化）
└── 竞争格局（管线对比表 + 时间线）
```

### 13.6 编码顺序与实际落地

1. ✅ 当前 MVP：`scripts/build-frontend-data.py` 生成 `landscape-data.js`
2. ✅ 前端：诊治格局页面已完成 4 标签页
3. ✅ 中外差异表格、治疗通路图、竞争管线表已接入
4. 后续增强：如需更细证据等级与竞争情报维护，再拆分 `extract-landscape-signals.py`、`build-evidence-matrix.py`、`build-competitive-table.py`

---

## 十四、阶段 6 详细设计（Dashboard + 整合）

> 设计日期：2026-06-16
> 状态：✅ 已完成（2026-06-17）
> 前置：阶段 1-5

### 14.1 Dashboard 四个区块

```
Dashboard（首页）
├── ① 本周信号（top 5，来自阶段 1 + 阶段 3）
├── ② 待处理工作（待读文献 / 待跟进 Follow-up / 待审模块更新）
├── ③ 快速入口（新建拜访 / 生成简报 / 检索知识库）
└── ④ 团队概览（拜访统计 + 共性趋势 + 最新产出）
```

### 14.2 信号优先级算法

```
0. 📋 模块更新建议（待确认）→ 最置顶
1. 🔴 强信号 → 置顶
2. ⚠️ 超时 Follow-up → 次置顶
3. 🟡 中信号 + 多人提到 → 普通
4. 🟢 弱信号 + 个人 Follow-up → 折叠
```

### 14.3 编码顺序与实际落地

1. ✅ 重构 `index.html`（四个区块）
2. ✅ 新增 `assets/dashboard.js`
3. ✅ 各区块跳转链接已接入全局导航

---

## 十五、阶段 7 详细设计（自动化流水线）

> 设计日期：2026-06-16
> 状态：✅ 自动化骨架已完成（2026-06-17）；GitHub Secrets 与外部通知待部署配置
> 前置：阶段 1-6
> 运行环境：**GitHub Actions**（推荐，与部署同源，免费额度充足）

### 15.1 管线顺序

**触发：GitHub Actions cron（北京时间每周日 23:00）**

```
每周日 23:00 cron 触发：

1.  fetch-pubmed-weekly.py → literature-full.json（增量）
2.  backfill-study-classification.py → 证据等级（P0 修复后）
3.  backfill-journal-metrics.py → IF/分区
4.  extract-signals.py → signals.json（LLM 逐篇）
5.  signal-aggregation → signals-weekly.js
6.  build-expert-profiles.py → expert-profiles.js
7.  build-china-intelligence.py → china-intelligence.js
8.  extract-landscape-signals.py → landscape-signals.json
9.  build-evidence-matrix.py → 证据矩阵更新
10. build-competitive-table.py → 竞争格局表
11. split-recent-data.py → recent.js
12. build-frontend-data.py / 各构建脚本 → signals/china/expert/landscape/modules 各 .js
13. module-update-check.py → 模块更新建议（LLM 语义判断）
14. git add + commit + push → GitHub Pages 自动部署
15. 通知（飞书 incoming webhook，独立于任何 AI 平台）
```

### 15.2 通知内容

```
MA-MG-HUB 本周更新（2026-06-16）

📚 新文献：23 篇（5 篇中国相关）
🔥 强信号：2 条
⚠️ 待跟进：3 项 Follow-up 临近 deadline
📋 模块更新建议：1 项

👉 查看详情：https://reiger-luo.github.io/MA-MG-HUB/
```

**通知渠道：** 使用标准飞书 incoming webhook，避免依赖特定 AI 助手平台的 `send_message` 工具；若 webhook 暂未配置，先输出本地日志和 GitHub Actions summary。

### 15.3 失败处理

| 场景 | 处理 |
|:--|:--|
| PubMed 限频 | 等待 60s 重试，最多 3 次 |
| LLM 超时 | 跳过该批次，记录日志，通知"部分信号未提取" |
| git push 失败 | 本地缓存，下次重试 |
| 前端加载失败 | 显示"数据更新中"提示 |

### 15.4 编码顺序与实际落地

1. ✅ 写 `scripts/run-weekly-pipeline.py`（统一调度）
2. ✅ 新增 `.github/workflows/weekly-pipeline.yml`（GitHub Actions cron + manual dispatch）
3. ✅ 本地缺少 `literature-full.json` 时自动回退到公开 `literature-recent.js`，避免 Actions 失败
4. 待部署配置：API key 放 Repository Secrets
5. 后续增强：飞书 webhook、失败告警、Actions summary

---

## 附录 A：现有资产盘点与 v3.2 对应（实施版）

> 数据规模沿用 2026-06-16 实测口径；实施状态更新于 2026-06-17。

### 已复用或保留

| 资产 | 类型 | v3.2 位置 | 说明 |
|:--|:--|:--|:--|
| `fetch-pubmed-weekly.py` | 脚本 | §15.1 步骤1 | 14天窗口，稳健 |
| `fetch-pubmed-full.py` | 脚本 | 一次性 | 已完成初始化 |
| `backfill-journal-metrics.py` | 脚本 | §15.1 步骤3 | curl+Ablesci；本地全库存在时由周更管线调用 |
| `split-recent-data.py` | 脚本 | §15.1 步骤11 | 职责保持为近1年文献切分与 `literature-recent.js` 输出 |
| `main.css` | 样式 | 全部前端 | 已扩展为 v3.2 工作站组件样式 |
| `data-ops.html` | 页面 | §五 | 已动态化，展示公开数据产物与流水线状态 |
| `literature-full.json` | 数据 | §10 | 10,577 篇，本地使用，gitignore 不入仓库 |
| `assets/journal_metrics.json` | 数据 | §9/§10 | 829期刊（600个有IF） |

### 已修复（✅ 阶段 0 已完成）

| 资产 | 原问题 | 修复 |
|:--|:--|:--|
| `backfill-study-classification.py` | main() 引用已删除的 `literature-2026.json` | ✅ 删除 yearly 分支，只回填 `literature-full.json` |
| `normalize-journal-names.py` | save_data() 写已删除的 `literature-2026.json` | ✅ 删除该写入逻辑 |
| `fetch-pubmed-*.py` DOI 解析 | 全库 DOI=0 | ⏭️ **不作为阻断项** — 引用溯源统一改用 PMID；DOI 后续作为可选增强字段补充 |

### 已落地新增资产

| 资产 | 阶段 | 说明 |
|:--|:--|:--|
| `scripts/build-frontend-data.py` | 阶段 1-6 | 统一生成 signals/china/expert/landscape/content/dashboard 前端数据 |
| `scripts/run-weekly-pipeline.py` | 阶段 7 | 统一调度周更流水线，支持 full 数据存在/缺失两种路径 |
| `.github/workflows/weekly-pipeline.yml` | 阶段 7 | GitHub Actions 周更与手动触发 |
| `data/signals-weekly.js` | 阶段 1 | 本周信号板数据 |
| `data/china-intelligence.js` | 阶段 1 | 中国情报数据 |
| `data/expert-profiles.js` | 阶段 2 | 专家画像公开字段 |
| `data/landscape-data.js` | 阶段 5 | 证据矩阵、治疗进展、中外差异、竞争管线 |
| `data/content-modules.js` | 阶段 4 | 内容工坊模板与模块 |
| `data/dashboard-data.js` | 阶段 6 | Dashboard 汇总数据 |
| `assets/dashboard.js` | 阶段 6 | Dashboard 交互 |
| `assets/knowledge.js` | 阶段 1/2/5 | 知识库统一检索 |
| `assets/msl.js` | 阶段 2/3 | 专家画像、拜访简报、Follow-up 本地闭环 |
| `assets/materials.js` | 阶段 4 | 内容工坊交互与合规检查 |
| `assets/landscape.js` | 阶段 5 | 诊治格局图表与表格 |

### 后续增强资产（不阻断当前静态站 MVP）

| 资产/能力 | 触发条件 |
|:--|:--|
| 拆分 `extract-signals.py` / `build-china-intelligence.py` | 需要高保真 LLM 信号提取与可解释日志时 |
| 拆分 `build-expert-profiles.py` | 需要人工专家名单、机构消歧和内部标签时 |
| `extract-module-data.py` / `module-manager.py` / `compliance-check.py` | 内容模块进入多人维护和版本管理时 |
| `extract-landscape-signals.py` / `build-evidence-matrix.py` / `build-competitive-table.py` | 诊治格局需要医学/商业复核后的结构化维护时 |
| 轻量后端（Cloudflare Workers / Vercel Functions） | MSL 本地 MVP 验证录入率和团队协作价值后 |
| 飞书 webhook / Actions summary | 部署环境配置完成后 |

### 已合并入口

| 资产 | 处理 |
|:--|:--|
| `pages/progress.html` | 保留跳转页，实际能力进入内容工坊 |
| `pages/competitive.html` | 保留跳转页，实际能力进入诊治格局 |
| `pages/outputs.html` | 保留跳转页，实际能力进入内容工坊 |

---

## 附录 B：数据资产规模（2026-06-16 实测）

### 文献库

| 指标 | 近1年 | 全库 | 说明 |
|:--|:--|:--|:--|
| 文献总量 | 1,154 | **10,577** | 含 PMID/标题/摘要/作者/期刊/entry_date |
| 有 IF | 960 (83%) | **7,119 (67%)** | journal_metrics.json 回填 |
| 有证据等级 | 834 (72%) | **7,204 (68%)** | study-classification 回填 |
| 中国相关 | 313 (27%) | **1,866 (17%)** | affiliation 判定 |
| 有摘要 | — | **9,235 (87%)** | |
| 有 DOI | — | **0 (0%)** | ⏭️ **不作为阻断项** — 引用溯源统一用 PMID；DOI 仅作为可选增强字段 |

### 证据等级分布（全库）

| 等级 | 篇数 |
|:--|:--|
| I（系统综述/指南） | 299 |
| II（RCT） | 423 |
| III（非随机对照） | 359 |
| IV（回顾/单臂） | 1,411 |
| V（病例报告） | 2,476 |
| VI（叙述综述） | 2,236 |
| Unclassified | 2,765（26%，建议 LLM 二次分类） |

### 研究类型 TOP10（全库）

| 类型 | 篇数 |
|:--|:--|
| Unclassified | 2,765 |
| Case Report | 2,476 |
| Review | 2,236 |
| Single Arm | 856 |
| Historical Control | 523 |
| RCT | 423 |
| Non-randomized controlled cohort | 359 |
| Animal Study | 286 |
| ITC | 163 |
| In Vitro | 143 |

### 作者与期刊

| 指标 | 数值 |
|:--|:--|
| 唯一作者 | 39,334 |
| 发文≥3 篇 | 3,686（专家画像候选池） |
| 发文≥10 篇 | 556（核心专家候选） |
| 发文≥20 篇 | 189（顶尖专家候选） |
| TOP1 作者 | Mantegazza Renato, 127 篇 |
| 全库唯一期刊 | 1,984 |
| journal_metrics cache | 829（其中 600 个有有效 IF） |

### 中国相关 TOP 机构（发文频次）

| 机构 | 篇数 |
|:--|:--|
| The First Affiliated Hospital（多家） | 92 |
| Xiangya Hospital | 54 |
| Tangdu Hospital | 39 |
| The Second Affiliated（多家） | 38 |

### 数据文件大小

| 文件 | 大小 | Git 状态 |
|:--|:--|:--|
| `literature-full.json` | 22.00 MB | gitignore（本地） |
| `literature-recent.json` | 2.79 MB | gitignore（本地） |
| `literature-recent.js` | 2.61 MB | 入仓库（前端加载） |
| `assets/journal_metrics.json` | 0.11 MB | 入仓库 |

---

## 附录 C：数据合规隔离规则（🆕）

| 数据类型 | 性质 | 存储位置 | Git 状态 |
|:--|:--|:--|:--|
| 文献数据（PubMed 公开） | 公开 | 本地 + .js 产物入仓库 | full/recent.json gitignore；recent.js 入仓库 |
| 期刊 IF/分区 | 公开 | assets/ | 入仓库 |
| 专家画像（发文/期刊/合作者） | 公开（PubMed 衍生） | expert-profiles.js | 入仓库（仅公开字段） |
| 专家内部标签（priority/msl_owner/tags） | 🔴 内部分类 | experts-core.json | **gitignore** |
| 拜访记录（讨论/观点/索要材料） | 🔴 商业敏感+个人信息 | MVP 本地 gitignore；验证后轻量后端（KV/D1） | **绝不入仓库** |
| 团队信号汇总（脱敏后） | 聚合脱敏 | 可入仓库 | 视脱敏程度 |
| LLM 缓存（含文献摘要 prompt） | 可能含敏感 | data/.llm_cache/ | **gitignore** |

**`.gitignore` 补充规则（阶段 0 实施）：**
```
# 现有
.DS_Store
data/literature-full.json
data/archive/
data/literature-recent.json

# v3.2 新增
data/experts-core.json
data/expert-internal.json
data/visit-records.json
data/follow-up-backlog.json
data/.llm_cache/
data/.llm_cost.log
```

---

## 附录 D：复用率诚实评估

| 层 | v3-clean 声称 | v3.2 实施结果 | 说明 |
|:--|:--|:--|:--|
| 数据管线层 | ~70% | **~80%** | fetch/backfill/split 健壮；两脚本已在阶段 0 修复 ✅ |
| LLM 引擎层 | （未单独评估） | **阶段 0 已建设** ✅ | `scripts/llm_client.py`（缓存+重试+成本日志），约 240 行 |
| 前端展示层 | ~70% | **已重建为 7 页面静态站 MVP** ✅ | 初始复用约 30%，v3.2 已完成 Dashboard、情报、知识库、MSL、内容工坊、诊治格局、数据状态 |
| 前端交互层 | （未单独评估） | **已补齐本地 MVP** ✅ | 拜访简报、Follow-up、内容组装、证据矩阵筛选均已落地 |
| **整体** | **~70%** | **静态站 MVP 已完成** ✅ | 后续重点从"建页面"转向数据质量、医学/合规复核、后端与通知配置 |

---

## 附录 E：阶段 0 实施记录（2026-06-17）

> 完整记录阶段 0 的实际产出，便于后续接手者快速了解基础设施现状。

### 产出文件

| 文件 | 操作 | 说明 |
|:--|:--|:--|
| `scripts/llm_client.py` | 🆕 新建 | 统一 LLM 客户端（~240 行）。DeepSeek 官方 API，OpenAI 兼容协议。特性：密钥自动加载（环境变量→`~/.config/mg-hub/.env`）、sha256 缓存（`data/.llm_cache/`）、指数退避重试（最多 3 次）、成本日志（`data/.llm_cost.log`）。模型：`DEFAULT=deepseek-chat`（日常提取）、`REASONER=deepseek-reasoner`（推理）。 |
| `scripts/llm_demo.py` | 🆕 新建 | 冒烟测试脚本。验证 API 连通→缓存写入→缓存命中→成本日志。 |
| `scripts/backfill-study-classification.py` | ✏️ 修复 | 删除 main() 中对已删除的 `literature-2026.json` 的引用（原 L154-162），简化为只回填 `literature-full.json`。 |
| `scripts/normalize-journal-names.py` | ✏️ 修复 | 删除 save_data() 中写 `literature-2026.json` 的逻辑（原 L38-40）。 |
| `.gitignore` | ✏️ 更新 | 追加 `.env`、`*.env`、`data/.llm_cache/`、`data/.llm_cost.log`。 |
| `~/.config/mg-hub/.env` | 🆕 新建 | DeepSeek API key 存放处。home 目录，权限 600，**不在仓库内**，不被 git 跟踪。 |

### 验证结果

| 验证项 | 结果 |
|:--|:--|
| `literature-2026.json` 引用清除 | ✅ `grep -rn "literature-202" scripts/` 无输出 |
| backfill-study-classification.py 语法 | ✅ `ast.parse` 通过 |
| normalize-journal-names.py 语法 | ✅ `ast.parse` 通过 |
| LLM API 连通 | ✅ DeepSeek 返回准确中文回复（1.1s） |
| 缓存命中 | ✅ 第二次调用 0.001s，两次回复一致 |
| 成本日志 | ✅ `data/.llm_cost.log` 已创建，单次 ¥0.0001 |
| 密钥/缓存 git 隔离 | ✅ `git check-ignore` 确认均被忽略 |

### 后续脚本使用方式

```python
# 任何脚本只需 import 即可调用 LLM
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent))
from llm_client import complete, REASONER, total_cost

# 日常提取/分类
reply = complete("分析这篇文献的信号", temperature=0.3)

# 需要推理的任务
reply = complete("...", model=REASONER)

# 查累计花费
print(f"已花费 ¥{total_cost()}")
```

---

*v3.2 规划与静态站 MVP 实施记录结束。配套审查报告见 `可行性审查报告.md`。*
